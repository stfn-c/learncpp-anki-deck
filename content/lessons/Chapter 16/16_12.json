{
  "url": "https://www.learncpp.com/cpp-tutorial/stdvector-bool/",
  "title": "16.12 \u2014 std::vector<bool>",
  "sections": [
    {
      "heading": null,
      "content": [
        {
          "text": "In lessonO.1 -- Bit flags and bit manipulation via std::bitset, we discussed howstd::bitsethas the capability to compact 8 Boolean values into a byte. Those bits can then be modified via the member functions ofstd::bitset.std::vectorhas an interesting trick up its sleeves. There is a special implementation forstd::vector<bool>that may be more space efficient for Boolean values by similarly compacting 8 Boolean values into a byte.For advanced readersWhen a template class has a different implementation for a particular template type argument, this is calledclass template specialization. We discuss this topic further in lesson26.4 -- Class template specialization.Unlikestd::bitset, which was designed for bit manipulation,std::vector<bool>lacks bit manipulation member functions.Usingstd::vector<bool>For the most part,std::vector<bool>works just like a normalstd::vector:#include <iostream>\n#include <vector>\n\nint main()\n{\n    std::vector<bool> v { true, false, false, true, true };\n    \n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    // Change the Boolean value with index 4 to false\n    v[4] = false;\n\n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    return 0;\n}On the author\u2019s 64-bit machine, this prints:1 0 0 1 1\n1 0 0 1 0std::vector<bool>tradeoffsHowever,std::vector<bool>has some tradeoffs that users should be aware of.First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "std::vectorhas an interesting trick up its sleeves. There is a special implementation forstd::vector<bool>that may be more space efficient for Boolean values by similarly compacting 8 Boolean values into a byte.For advanced readersWhen a template class has a different implementation for a particular template type argument, this is calledclass template specialization. We discuss this topic further in lesson26.4 -- Class template specialization.Unlikestd::bitset, which was designed for bit manipulation,std::vector<bool>lacks bit manipulation member functions.Usingstd::vector<bool>For the most part,std::vector<bool>works just like a normalstd::vector:#include <iostream>\n#include <vector>\n\nint main()\n{\n    std::vector<bool> v { true, false, false, true, true };\n    \n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    // Change the Boolean value with index 4 to false\n    v[4] = false;\n\n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    return 0;\n}On the author\u2019s 64-bit machine, this prints:1 0 0 1 1\n1 0 0 1 0std::vector<bool>tradeoffsHowever,std::vector<bool>has some tradeoffs that users should be aware of.First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "For advanced readersWhen a template class has a different implementation for a particular template type argument, this is calledclass template specialization. We discuss this topic further in lesson26.4 -- Class template specialization."
        },
        {
          "text": "When a template class has a different implementation for a particular template type argument, this is calledclass template specialization. We discuss this topic further in lesson26.4 -- Class template specialization."
        },
        {
          "text": "Unlikestd::bitset, which was designed for bit manipulation,std::vector<bool>lacks bit manipulation member functions.Usingstd::vector<bool>For the most part,std::vector<bool>works just like a normalstd::vector:#include <iostream>\n#include <vector>\n\nint main()\n{\n    std::vector<bool> v { true, false, false, true, true };\n    \n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    // Change the Boolean value with index 4 to false\n    v[4] = false;\n\n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    return 0;\n}On the author\u2019s 64-bit machine, this prints:1 0 0 1 1\n1 0 0 1 0std::vector<bool>tradeoffsHowever,std::vector<bool>has some tradeoffs that users should be aware of.First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "Usingstd::vector<bool>For the most part,std::vector<bool>works just like a normalstd::vector:#include <iostream>\n#include <vector>\n\nint main()\n{\n    std::vector<bool> v { true, false, false, true, true };\n    \n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    // Change the Boolean value with index 4 to false\n    v[4] = false;\n\n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    return 0;\n}On the author\u2019s 64-bit machine, this prints:1 0 0 1 1\n1 0 0 1 0std::vector<bool>tradeoffsHowever,std::vector<bool>has some tradeoffs that users should be aware of.First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "For the most part,std::vector<bool>works just like a normalstd::vector:#include <iostream>\n#include <vector>\n\nint main()\n{\n    std::vector<bool> v { true, false, false, true, true };\n    \n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    // Change the Boolean value with index 4 to false\n    v[4] = false;\n\n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    return 0;\n}On the author\u2019s 64-bit machine, this prints:1 0 0 1 1\n1 0 0 1 0std::vector<bool>tradeoffsHowever,std::vector<bool>has some tradeoffs that users should be aware of.First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "code": "#include <iostream>\n#include <vector>\n\nint main()\n{\n    std::vector<bool> v { true, false, false, true, true };\n    \n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    // Change the Boolean value with index 4 to false\n    v[4] = false;\n\n    for (int i : v)\n        std::cout << i << ' ';\n    std::cout << '\\n';\n\n    return 0;\n}"
        },
        {
          "text": "On the author\u2019s 64-bit machine, this prints:1 0 0 1 1\n1 0 0 1 0std::vector<bool>tradeoffsHowever,std::vector<bool>has some tradeoffs that users should be aware of.First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "code": "1 0 0 1 1\n1 0 0 1 0\n"
        },
        {
          "text": "std::vector<bool>tradeoffsHowever,std::vector<bool>has some tradeoffs that users should be aware of.First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "However,std::vector<bool>has some tradeoffs that users should be aware of.First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "First,std::vector<bool>has a fairly high amount of overhead (sizeof(std::vector<bool>)is 40 bytes on the author\u2019s machine), so you won\u2019t save memory unless you\u2019re allocating more Boolean values than the overhead for your architecture.Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "Second, the performance ofstd::vector<bool>is highly dependent upon the implementation (as implementations aren\u2019t even required to do optimization, let alone do it well). Perthis article, a highly optimized implementation can be significantly faster than alternatives. However, a poorly optimized implementation will be slower.Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "Third and most importantly,std::vector<bool>is not a vector (it is not required to be contiguous in memory), nor does it holdboolvalues (it holds a collection of bits), nor does it meet C++\u2019s definition of a container.Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "Althoughstd::vector<bool>behaves like a vector in most cases, it is not fully compatible with the rest of the standard library. Code that works with other element types may not work withstd::vector<bool>.For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "For example, the following code works whenTis any type exceptbool:template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "code": "template<typename T>\nvoid foo( std::vector<T>& v )\n{\n    T& first = v[0]; // get a reference to the first element\n    // Do something with first\n}"
        },
        {
          "text": "Avoidstd::vector<bool>The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "The modern consensus is thatstd::vector<bool>should generally be avoided, as the performance gains are unlikely to be worth the incompatibility headaches due to it not being a proper container.Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "Unfortunately, this optimizing version ofstd::vector<bool>is enabled by default, and there is no way to disable it in favor of a non-optimized version that is actually a container. There have been calls to deprecatestd::vector<bool>, and work is underway to determine what a replacement compacted vector ofboolmight look like (perhaps as a futurestd::dynamic_bitset).Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "text": "Our recommendation is as follows:Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>.Next lesson16.xChapter 16 summary and quizBack to table of contentsPrevious lesson16.11std::vector and stack behavior"
        },
        {
          "list": [
            "Use (constexpr)std::bitsetwhen the number of bits you need is known at compile-time, you don\u2019t have more than a moderate number of Boolean values to store (e.g. under 64k), and the limited set of operators and member functions (e.g. lack of iterator support) meets your requirements.Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.",
            "Preferstd::vector<char>when you need a resizable container of Boolean values and space-savings isn\u2019t a necessity. This type behaves like a normal container.Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t.",
            "Favor a 3rd party implementation of a dynamic bitset (such asboost::dynamic_bitset) when you need a dynamic bitset to do bit operations on. Such types won\u2019t pretend to be standard library containers when they aren\u2019t."
          ]
        },
        {
          "text": "Best practiceFavorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>."
        },
        {
          "text": "Favorconstexpr std::bitset,std::vector<char>, or 3rd party dynamic bitsets overstd::vector<bool>."
        }
      ]
    }
  ],
  "quiz": []
}