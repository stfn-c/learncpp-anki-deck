{
  "url": "https://www.learncpp.com/cpp-tutorial/stdstring-construction-and-destruction/",
  "title": "22.2 \u2014 std::string construction and destruction",
  "sections": [
    {
      "heading": null,
      "content": [
        {
          "text": "In this lesson, we\u2019ll take a look at how to construct objects of std::string, as well as how to create strings from numbers and vice-versa.String constructionThe string classes have a number of constructors that can be used to create strings. We\u2019ll take a look at each of them here.Note: string::size_type resolves to size_t, which is the same unsigned integral type that is returned by the sizeof operator. The actual size of size_t depending on the environment. For the purposes of this tutorial, envision it as an unsigned int.string::string()This is the default constructor. It creates an empty string.Sample code:std::string sSource;\nstd::cout << sSource;Output:string::string(const string& strString)This is the copy constructor. This constructor creates a new string as a copy of strString.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;Output:my stringstring::string(const string& strString, size_type unIndex)string::string(const string& strString, size_type unIndex, size_type unLength)This constructor creates a new string that contains at most unLength characters from strString, starting with index unIndex. If a NULL is encountered, the string copy will end, even if unLength has not been reached.If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';Output:string\nstristring::string(const char* szCString)This constructor creates a new string from the C-style string szCString, up to but not including the NULL terminator.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';Output:my stringstring::string(const char* szCString, size_type unLength)This constructor creates a new string from the first unLength chars from the C-style string szCString.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';Output:my sstring::string(size_type nNum, char chChar)This constructor creates a new string initialized by nNum occurances of the character chChar.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Sample code:std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';Output:QQQQtemplatestring::string(InputIterator itBeg, InputIterator itEnd)This constructor creates a new string initialized by the characters of range [itBeg, itEnd).If the resulting size exceeds the maximum string length, the length_error exception will be thrown.No sample code for this one. It\u2019s obscure enough you\u2019ll probably never use it.string::~string()String destructionThis is the destructor. It destroys the string and frees the memory.No sample code here either since the destructor isn\u2019t called explicitly.Constructing strings from numbersOne notable omission in the std::string class is the lack of ability to create strings from numbers. For example:std::string sFour{ 4 };Produces the following error:c:vcprojectstest2test2test.cpp(10) : error C2664: 'std::basic_string<_Elem,_Traits,_Ax>::basic_string(std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it)' : cannot convert parameter 1 from 'int' to 'std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it'Remember what I said about the string classes producing horrible looking errors? The relevant bit of information here is:cannot convert parameter 1 from 'int' to 'std::basic_stringIn other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "String constructionThe string classes have a number of constructors that can be used to create strings. We\u2019ll take a look at each of them here.Note: string::size_type resolves to size_t, which is the same unsigned integral type that is returned by the sizeof operator. The actual size of size_t depending on the environment. For the purposes of this tutorial, envision it as an unsigned int.string::string()This is the default constructor. It creates an empty string.Sample code:std::string sSource;\nstd::cout << sSource;Output:string::string(const string& strString)This is the copy constructor. This constructor creates a new string as a copy of strString.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;Output:my stringstring::string(const string& strString, size_type unIndex)string::string(const string& strString, size_type unIndex, size_type unLength)This constructor creates a new string that contains at most unLength characters from strString, starting with index unIndex. If a NULL is encountered, the string copy will end, even if unLength has not been reached.If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';Output:string\nstristring::string(const char* szCString)This constructor creates a new string from the C-style string szCString, up to but not including the NULL terminator.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';Output:my stringstring::string(const char* szCString, size_type unLength)This constructor creates a new string from the first unLength chars from the C-style string szCString.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';Output:my sstring::string(size_type nNum, char chChar)This constructor creates a new string initialized by nNum occurances of the character chChar.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Sample code:std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';Output:QQQQtemplatestring::string(InputIterator itBeg, InputIterator itEnd)This constructor creates a new string initialized by the characters of range [itBeg, itEnd).If the resulting size exceeds the maximum string length, the length_error exception will be thrown.No sample code for this one. It\u2019s obscure enough you\u2019ll probably never use it.string::~string()String destructionThis is the destructor. It destroys the string and frees the memory.No sample code here either since the destructor isn\u2019t called explicitly.Constructing strings from numbersOne notable omission in the std::string class is the lack of ability to create strings from numbers. For example:std::string sFour{ 4 };Produces the following error:c:vcprojectstest2test2test.cpp(10) : error C2664: 'std::basic_string<_Elem,_Traits,_Ax>::basic_string(std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it)' : cannot convert parameter 1 from 'int' to 'std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it'Remember what I said about the string classes producing horrible looking errors? The relevant bit of information here is:cannot convert parameter 1 from 'int' to 'std::basic_stringIn other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "The string classes have a number of constructors that can be used to create strings. We\u2019ll take a look at each of them here.Note: string::size_type resolves to size_t, which is the same unsigned integral type that is returned by the sizeof operator. The actual size of size_t depending on the environment. For the purposes of this tutorial, envision it as an unsigned int.string::string()This is the default constructor. It creates an empty string.Sample code:std::string sSource;\nstd::cout << sSource;Output:string::string(const string& strString)This is the copy constructor. This constructor creates a new string as a copy of strString.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;Output:my stringstring::string(const string& strString, size_type unIndex)string::string(const string& strString, size_type unIndex, size_type unLength)This constructor creates a new string that contains at most unLength characters from strString, starting with index unIndex. If a NULL is encountered, the string copy will end, even if unLength has not been reached.If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';Output:string\nstristring::string(const char* szCString)This constructor creates a new string from the C-style string szCString, up to but not including the NULL terminator.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';Output:my stringstring::string(const char* szCString, size_type unLength)This constructor creates a new string from the first unLength chars from the C-style string szCString.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';Output:my sstring::string(size_type nNum, char chChar)This constructor creates a new string initialized by nNum occurances of the character chChar.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Sample code:std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';Output:QQQQtemplatestring::string(InputIterator itBeg, InputIterator itEnd)This constructor creates a new string initialized by the characters of range [itBeg, itEnd).If the resulting size exceeds the maximum string length, the length_error exception will be thrown.No sample code for this one. It\u2019s obscure enough you\u2019ll probably never use it.string::~string()String destructionThis is the destructor. It destroys the string and frees the memory.No sample code here either since the destructor isn\u2019t called explicitly.Constructing strings from numbersOne notable omission in the std::string class is the lack of ability to create strings from numbers. For example:std::string sFour{ 4 };Produces the following error:c:vcprojectstest2test2test.cpp(10) : error C2664: 'std::basic_string<_Elem,_Traits,_Ax>::basic_string(std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it)' : cannot convert parameter 1 from 'int' to 'std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it'Remember what I said about the string classes producing horrible looking errors? The relevant bit of information here is:cannot convert parameter 1 from 'int' to 'std::basic_stringIn other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "Note: string::size_type resolves to size_t, which is the same unsigned integral type that is returned by the sizeof operator. The actual size of size_t depending on the environment. For the purposes of this tutorial, envision it as an unsigned int.string::string()This is the default constructor. It creates an empty string.Sample code:std::string sSource;\nstd::cout << sSource;Output:string::string(const string& strString)This is the copy constructor. This constructor creates a new string as a copy of strString.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;Output:my stringstring::string(const string& strString, size_type unIndex)string::string(const string& strString, size_type unIndex, size_type unLength)This constructor creates a new string that contains at most unLength characters from strString, starting with index unIndex. If a NULL is encountered, the string copy will end, even if unLength has not been reached.If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';Output:string\nstristring::string(const char* szCString)This constructor creates a new string from the C-style string szCString, up to but not including the NULL terminator.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';Output:my stringstring::string(const char* szCString, size_type unLength)This constructor creates a new string from the first unLength chars from the C-style string szCString.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';Output:my sstring::string(size_type nNum, char chChar)This constructor creates a new string initialized by nNum occurances of the character chChar.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Sample code:std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';Output:QQQQtemplatestring::string(InputIterator itBeg, InputIterator itEnd)This constructor creates a new string initialized by the characters of range [itBeg, itEnd).If the resulting size exceeds the maximum string length, the length_error exception will be thrown.No sample code for this one. It\u2019s obscure enough you\u2019ll probably never use it.string::~string()String destructionThis is the destructor. It destroys the string and frees the memory.No sample code here either since the destructor isn\u2019t called explicitly.Constructing strings from numbersOne notable omission in the std::string class is the lack of ability to create strings from numbers. For example:std::string sFour{ 4 };Produces the following error:c:vcprojectstest2test2test.cpp(10) : error C2664: 'std::basic_string<_Elem,_Traits,_Ax>::basic_string(std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it)' : cannot convert parameter 1 from 'int' to 'std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it'Remember what I said about the string classes producing horrible looking errors? The relevant bit of information here is:cannot convert parameter 1 from 'int' to 'std::basic_stringIn other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "string::string()This is the default constructor. It creates an empty string.Sample code:std::string sSource;\nstd::cout << sSource;Output:"
        },
        {
          "text": "string::string()This is the default constructor. It creates an empty string.Sample code:std::string sSource;\nstd::cout << sSource;Output:"
        },
        {
          "text": "This is the default constructor. It creates an empty string.Sample code:std::string sSource;\nstd::cout << sSource;Output:"
        },
        {
          "list": [
            "This is the default constructor. It creates an empty string."
          ]
        },
        {
          "text": "Sample code:std::string sSource;\nstd::cout << sSource;Output:"
        },
        {
          "code": "std::string sSource;\nstd::cout << sSource;"
        },
        {
          "code": ""
        },
        {
          "text": "string::string(const string& strString)This is the copy constructor. This constructor creates a new string as a copy of strString.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;Output:my string"
        },
        {
          "text": "string::string(const string& strString)This is the copy constructor. This constructor creates a new string as a copy of strString.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;Output:my string"
        },
        {
          "text": "This is the copy constructor. This constructor creates a new string as a copy of strString.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;Output:my string"
        },
        {
          "list": [
            "This is the copy constructor. This constructor creates a new string as a copy of strString."
          ]
        },
        {
          "text": "Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;Output:my string"
        },
        {
          "code": "std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource };\nstd::cout << sOutput;"
        },
        {
          "text": "Output:my string"
        },
        {
          "code": "my string\n"
        },
        {
          "text": "string::string(const string& strString, size_type unIndex)string::string(const string& strString, size_type unIndex, size_type unLength)This constructor creates a new string that contains at most unLength characters from strString, starting with index unIndex. If a NULL is encountered, the string copy will end, even if unLength has not been reached.If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';Output:string\nstri"
        },
        {
          "text": "string::string(const string& strString, size_type unIndex)string::string(const string& strString, size_type unIndex, size_type unLength)This constructor creates a new string that contains at most unLength characters from strString, starting with index unIndex. If a NULL is encountered, the string copy will end, even if unLength has not been reached.If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';Output:string\nstri"
        },
        {
          "text": "This constructor creates a new string that contains at most unLength characters from strString, starting with index unIndex. If a NULL is encountered, the string copy will end, even if unLength has not been reached.If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';Output:string\nstri"
        },
        {
          "list": [
            "This constructor creates a new string that contains at most unLength characters from strString, starting with index unIndex. If a NULL is encountered, the string copy will end, even if unLength has not been reached.If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.",
            "If no unLength is supplied, all characters starting from unIndex will be used.If unIndex is larger than the size of the string, the out_of_range exception will be thrown.",
            "If unIndex is larger than the size of the string, the out_of_range exception will be thrown."
          ]
        },
        {
          "text": "Sample code:std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';Output:string\nstri"
        },
        {
          "code": "std::string sSource{ \"my string\" };\nstd::string sOutput{ sSource, 3 };\nstd::cout << sOutput<< '\\n';\nstd::string sOutput2(sSource, 3, 4);\nstd::cout << sOutput2 << '\\n';"
        },
        {
          "text": "Output:string\nstri"
        },
        {
          "code": "string\nstri\n"
        },
        {
          "text": "string::string(const char* szCString)This constructor creates a new string from the C-style string szCString, up to but not including the NULL terminator.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';Output:my string"
        },
        {
          "text": "string::string(const char* szCString)This constructor creates a new string from the C-style string szCString, up to but not including the NULL terminator.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';Output:my string"
        },
        {
          "text": "This constructor creates a new string from the C-style string szCString, up to but not including the NULL terminator.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';Output:my string"
        },
        {
          "list": [
            "This constructor creates a new string from the C-style string szCString, up to but not including the NULL terminator.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.",
            "If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:szCString must not be NULL.",
            "Warning:szCString must not be NULL."
          ]
        },
        {
          "text": "Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';Output:my string"
        },
        {
          "code": "const char* szSource{ \"my string\" };\nstd::string sOutput{ szSource };\nstd::cout << sOutput << '\\n';"
        },
        {
          "text": "Output:my string"
        },
        {
          "code": "my string\n"
        },
        {
          "text": "string::string(const char* szCString, size_type unLength)This constructor creates a new string from the first unLength chars from the C-style string szCString.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';Output:my s"
        },
        {
          "text": "string::string(const char* szCString, size_type unLength)This constructor creates a new string from the first unLength chars from the C-style string szCString.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';Output:my s"
        },
        {
          "text": "This constructor creates a new string from the first unLength chars from the C-style string szCString.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';Output:my s"
        },
        {
          "list": [
            "This constructor creates a new string from the first unLength chars from the C-style string szCString.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!",
            "If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!",
            "Warning:For this function only, NULLs are not treated as end-of-string characters in szCString! This means it is possible to read off the end of your string if unLength is too big. Be careful not to overflow your string buffer!"
          ]
        },
        {
          "text": "Sample code:const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';Output:my s"
        },
        {
          "code": "const char* szSource{ \"my string\" };\nstd::string sOutput(szSource, 4);\nstd::cout << sOutput << '\\n';"
        },
        {
          "text": "Output:my s"
        },
        {
          "code": "my s\n"
        },
        {
          "text": "string::string(size_type nNum, char chChar)This constructor creates a new string initialized by nNum occurances of the character chChar.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Sample code:std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';Output:QQQQ"
        },
        {
          "text": "string::string(size_type nNum, char chChar)This constructor creates a new string initialized by nNum occurances of the character chChar.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Sample code:std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';Output:QQQQ"
        },
        {
          "text": "This constructor creates a new string initialized by nNum occurances of the character chChar.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.Sample code:std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';Output:QQQQ"
        },
        {
          "list": [
            "This constructor creates a new string initialized by nNum occurances of the character chChar.If the resulting size exceeds the maximum string length, the length_error exception will be thrown.",
            "If the resulting size exceeds the maximum string length, the length_error exception will be thrown."
          ]
        },
        {
          "text": "Sample code:std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';Output:QQQQ"
        },
        {
          "code": "std::string sOutput(4, 'Q');\nstd::cout << sOutput << '\\n';"
        },
        {
          "text": "Output:QQQQ"
        },
        {
          "code": "QQQQ\n"
        },
        {
          "text": "templatestring::string(InputIterator itBeg, InputIterator itEnd)This constructor creates a new string initialized by the characters of range [itBeg, itEnd).If the resulting size exceeds the maximum string length, the length_error exception will be thrown.No sample code for this one. It\u2019s obscure enough you\u2019ll probably never use it."
        },
        {
          "text": "templatestring::string(InputIterator itBeg, InputIterator itEnd)This constructor creates a new string initialized by the characters of range [itBeg, itEnd).If the resulting size exceeds the maximum string length, the length_error exception will be thrown.No sample code for this one. It\u2019s obscure enough you\u2019ll probably never use it."
        },
        {
          "text": "This constructor creates a new string initialized by the characters of range [itBeg, itEnd).If the resulting size exceeds the maximum string length, the length_error exception will be thrown.No sample code for this one. It\u2019s obscure enough you\u2019ll probably never use it."
        },
        {
          "list": [
            "This constructor creates a new string initialized by the characters of range [itBeg, itEnd).If the resulting size exceeds the maximum string length, the length_error exception will be thrown.",
            "If the resulting size exceeds the maximum string length, the length_error exception will be thrown."
          ]
        },
        {
          "text": "No sample code for this one. It\u2019s obscure enough you\u2019ll probably never use it."
        },
        {
          "text": "string::~string()String destructionThis is the destructor. It destroys the string and frees the memory.No sample code here either since the destructor isn\u2019t called explicitly."
        },
        {
          "text": "string::~string()String destructionThis is the destructor. It destroys the string and frees the memory.No sample code here either since the destructor isn\u2019t called explicitly."
        },
        {
          "text": "String destructionThis is the destructor. It destroys the string and frees the memory.No sample code here either since the destructor isn\u2019t called explicitly."
        },
        {
          "text": "String destructionThis is the destructor. It destroys the string and frees the memory.No sample code here either since the destructor isn\u2019t called explicitly."
        },
        {
          "list": [
            "This is the destructor. It destroys the string and frees the memory."
          ]
        },
        {
          "text": "No sample code here either since the destructor isn\u2019t called explicitly."
        },
        {
          "text": "Constructing strings from numbersOne notable omission in the std::string class is the lack of ability to create strings from numbers. For example:std::string sFour{ 4 };Produces the following error:c:vcprojectstest2test2test.cpp(10) : error C2664: 'std::basic_string<_Elem,_Traits,_Ax>::basic_string(std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it)' : cannot convert parameter 1 from 'int' to 'std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it'Remember what I said about the string classes producing horrible looking errors? The relevant bit of information here is:cannot convert parameter 1 from 'int' to 'std::basic_stringIn other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "One notable omission in the std::string class is the lack of ability to create strings from numbers. For example:std::string sFour{ 4 };Produces the following error:c:vcprojectstest2test2test.cpp(10) : error C2664: 'std::basic_string<_Elem,_Traits,_Ax>::basic_string(std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it)' : cannot convert parameter 1 from 'int' to 'std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it'Remember what I said about the string classes producing horrible looking errors? The relevant bit of information here is:cannot convert parameter 1 from 'int' to 'std::basic_stringIn other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "    std::string sFour{ 4 };"
        },
        {
          "text": "Produces the following error:c:vcprojectstest2test2test.cpp(10) : error C2664: 'std::basic_string<_Elem,_Traits,_Ax>::basic_string(std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it)' : cannot convert parameter 1 from 'int' to 'std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it'Remember what I said about the string classes producing horrible looking errors? The relevant bit of information here is:cannot convert parameter 1 from 'int' to 'std::basic_stringIn other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "c:vcprojectstest2test2test.cpp(10) : error C2664: 'std::basic_string<_Elem,_Traits,_Ax>::basic_string(std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it)' : cannot convert parameter 1 from 'int' to 'std::basic_string<_Elem,_Traits,_Ax>::_Has_debug_it'\n"
        },
        {
          "text": "Remember what I said about the string classes producing horrible looking errors? The relevant bit of information here is:cannot convert parameter 1 from 'int' to 'std::basic_stringIn other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "cannot convert parameter 1 from 'int' to 'std::basic_string"
        },
        {
          "text": "In other words, it tried to convert your int into a string but failed.The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "The easiest way to convert numbers into strings is to involve the std::ostringstream class. std::ostringstream is already set up to accept input from a variety of sources, including characters, numbers, strings, etc\u2026 It is also capable of outputting strings (either via the extraction operator>>, or via the str() function). For more information on std::ostringstream, see28.4 -- Stream classes for strings.Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "Here\u2019s a simple solution for creating std::string from various types of inputs:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline std::string ToString(T tX)\n{\n    std::ostringstream oStream;\n    oStream << tX;\n    return oStream.str();\n}"
        },
        {
          "text": "Here\u2019s some sample code to test it:int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "int main()\n{\n    std::string sFour{ ToString(4) };\n    std::string sSixPointSeven{ ToString(6.7) };\n    std::string sA{ ToString('A') };\n    std::cout << sFour << '\\n';\n    std::cout << sSixPointSeven << '\\n';\n    std::cout << sA << '\\n';\n}"
        },
        {
          "text": "And the output:4\n6.7\nANote that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "4\n6.7\nA\n"
        },
        {
          "text": "Note that this solution omits any error checking. It is possible that inserting tX into oStream could fail. An appropriate response would be to throw an exception if the conversion fails.Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere.Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "Related contentThe standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere."
        },
        {
          "text": "The standard library also contains a function namedstd::to_string()that can be used to convert numbers into a std::string. While this is a simpler solution for basic cases, the output of std::to_string may differ from the output of std::cout or our ToString() function above. Some of these differences are currently documentedhere."
        },
        {
          "text": "Converting strings to numbersSimilar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "text": "Similar to the solution above:#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "#include <iostream>\n#include <sstream>\n#include <string>\n \ntemplate <typename T>\ninline bool FromString(const std::string& sString, T& tX)\n{\n    std::istringstream iStream(sString);\n    return !(iStream >> tX).fail(); // extract value into tX, return success or not\n}"
        },
        {
          "text": "Here\u2019s some sample code to test it:int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "int main()\n{\n    double dX;\n    if (FromString(\"3.4\", dX))\n        std::cout << dX << '\\n'; \n    if (FromString(\"ABC\", dX))\n        std::cout << dX << '\\n'; \n}"
        },
        {
          "text": "And the output:3.4Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        },
        {
          "code": "3.4\n"
        },
        {
          "text": "Note that the second conversion failed and returned false.Next lesson22.3std::string length and capacityBack to table of contentsPrevious lesson22.1std::string and std::wstring"
        }
      ]
    }
  ],
  "quiz": []
}