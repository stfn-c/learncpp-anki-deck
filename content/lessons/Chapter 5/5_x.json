{
  "url": "https://www.learncpp.com/cpp-tutorial/chapter-5-summary-and-quiz/",
  "title": "5.x \u2014 Chapter 5 summary and quiz",
  "sections": [
    {
      "heading": null,
      "content": [
        {
          "text": "Chapter ReviewAconstantis a value that may not be changed during the program\u2019s execution. C++ supports two types of constants: named constants, and literals.Anamed constantis a constant value that is associated with an identifier. ALiteral constantis a constant value not associated with an identifier.A variable whose value can not be changed is called aconstant variable. Theconstkeyword can be used to make a variable constant. Constant variables must be initialized. Avoid usingconstwhen passing by value or returning by value.Atype qualifieris a keyword that is applied to a type that modifies how that type behaves. As of C++23, C++ only supportsconstandvolatileas type qualifiers.Aconstant expressionis an expression that can be evaluated at compile-time. An expression that is not a constant expression is sometimes called aruntime expression.Acompile-time constantis a constant whose value is known at compile-time. Aruntime constantis a constant whose initialization value isn\u2019t known until runtime.Aconstexprvariable must be a compile-time constant, and initialized with a constant expression. Function parameters cannot be constexpr.Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Aconstantis a value that may not be changed during the program\u2019s execution. C++ supports two types of constants: named constants, and literals.Anamed constantis a constant value that is associated with an identifier. ALiteral constantis a constant value not associated with an identifier.A variable whose value can not be changed is called aconstant variable. Theconstkeyword can be used to make a variable constant. Constant variables must be initialized. Avoid usingconstwhen passing by value or returning by value.Atype qualifieris a keyword that is applied to a type that modifies how that type behaves. As of C++23, C++ only supportsconstandvolatileas type qualifiers.Aconstant expressionis an expression that can be evaluated at compile-time. An expression that is not a constant expression is sometimes called aruntime expression.Acompile-time constantis a constant whose value is known at compile-time. Aruntime constantis a constant whose initialization value isn\u2019t known until runtime.Aconstexprvariable must be a compile-time constant, and initialized with a constant expression. Function parameters cannot be constexpr.Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Anamed constantis a constant value that is associated with an identifier. ALiteral constantis a constant value not associated with an identifier.A variable whose value can not be changed is called aconstant variable. Theconstkeyword can be used to make a variable constant. Constant variables must be initialized. Avoid usingconstwhen passing by value or returning by value.Atype qualifieris a keyword that is applied to a type that modifies how that type behaves. As of C++23, C++ only supportsconstandvolatileas type qualifiers.Aconstant expressionis an expression that can be evaluated at compile-time. An expression that is not a constant expression is sometimes called aruntime expression.Acompile-time constantis a constant whose value is known at compile-time. Aruntime constantis a constant whose initialization value isn\u2019t known until runtime.Aconstexprvariable must be a compile-time constant, and initialized with a constant expression. Function parameters cannot be constexpr.Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "A variable whose value can not be changed is called aconstant variable. Theconstkeyword can be used to make a variable constant. Constant variables must be initialized. Avoid usingconstwhen passing by value or returning by value.Atype qualifieris a keyword that is applied to a type that modifies how that type behaves. As of C++23, C++ only supportsconstandvolatileas type qualifiers.Aconstant expressionis an expression that can be evaluated at compile-time. An expression that is not a constant expression is sometimes called aruntime expression.Acompile-time constantis a constant whose value is known at compile-time. Aruntime constantis a constant whose initialization value isn\u2019t known until runtime.Aconstexprvariable must be a compile-time constant, and initialized with a constant expression. Function parameters cannot be constexpr.Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Atype qualifieris a keyword that is applied to a type that modifies how that type behaves. As of C++23, C++ only supportsconstandvolatileas type qualifiers.Aconstant expressionis an expression that can be evaluated at compile-time. An expression that is not a constant expression is sometimes called aruntime expression.Acompile-time constantis a constant whose value is known at compile-time. Aruntime constantis a constant whose initialization value isn\u2019t known until runtime.Aconstexprvariable must be a compile-time constant, and initialized with a constant expression. Function parameters cannot be constexpr.Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Aconstant expressionis an expression that can be evaluated at compile-time. An expression that is not a constant expression is sometimes called aruntime expression.Acompile-time constantis a constant whose value is known at compile-time. Aruntime constantis a constant whose initialization value isn\u2019t known until runtime.Aconstexprvariable must be a compile-time constant, and initialized with a constant expression. Function parameters cannot be constexpr.Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Acompile-time constantis a constant whose value is known at compile-time. Aruntime constantis a constant whose initialization value isn\u2019t known until runtime.Aconstexprvariable must be a compile-time constant, and initialized with a constant expression. Function parameters cannot be constexpr.Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Aconstexprvariable must be a compile-time constant, and initialized with a constant expression. Function parameters cannot be constexpr.Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Literalsare values inserted directly into the code. Literals have types, and literal suffixes can be used to change the type of a literal from the default type.Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Amagic numberis a literal (usually a number) that either has an unclear meaning or may need to be changed later. Don\u2019t use magic numbers in your code. Instead, use symbolic constants.In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "In everyday life, we count usingdecimalnumbers, which have 10 digits. Computers usebinary, which only has 2 digits. C++ also supportsoctal(base 8) andhexadecimal(base 16). These are all examples ofnumeral systems, which are collections of symbols (digits) used to represent numbers.Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Astringis a collection of sequential characters that is used to represent text (such as names, words, and sentences). String literals are always placed between double quotes. String literals in C++ are C-style strings, which have a strange type that is hard to work with.std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "std::stringoffers an easy and safe way to deal with text strings. std::string lives in the <string> header.std::stringis expensive to initialize (or assign to) and copy.std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "std::string_viewprovides read-only access to an existing string (a C-style string literal, a std::string, or a char array) without making a copy. Astd::string_viewthat is viewing a string that has been destroyed is sometimes called adanglingview. When astd::stringis modified, all views into thatstd::stringareinvalidated, meaning those views are now invalid. Using an invalidated view (other than to revalidate it) will produce undefined behavior.Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Because C-style string literals exist for the entire program, it is okay to set astd::string_viewto a C-style string literal, and even return such astd::string_viewfrom a function.Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Asubstringis a contiguous sequence of characters within an existing string.Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Quiz timeQuestion #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Question #1Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Why are named constants often a better choice than literal constants?Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Show SolutionUsing literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used.Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Using literal constants (a.k.a. magic numbers) in your program makes your program harder to understand and harder to modify. Symbolic constants help document what the numbers actually represent, and changing a symbolic constant at its declaration changes the value everywhere it is used."
        },
        {
          "text": "Why are const/constexpr variables usually a better choice than #defined symbolic constants?Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Show Solution#define constants do not show up in the debugger and are more likely to have naming conflicts.Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "#define constants do not show up in the debugger and are more likely to have naming conflicts."
        },
        {
          "text": "Question #2Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Find 3 issues in the following code:#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "code": "#include <cstdint> // for std::uint8_t\n#include <iostream>\n\nint main()\n{\n  std::cout << \"How old are you?\\n\";\n\n  std::uint8_t age{};\n  std::cin >> age;\n\n  std::cout << \"Allowed to drive a car in Texas: \";\n\n  if (age >= 16)\n      std::cout << \"Yes\";\n  else\n      std::cout << \"No\";\n\n  std::cout << '.\\n';\n\n  return 0;\n}"
        },
        {
          "text": "Sample desired output:How old are you?\n6\nAllowed to drive a car in Texas: NoHow old are you?\n19\nAllowed to drive a car in Texas: YesShow SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "code": "How old are you?\n6\nAllowed to drive a car in Texas: No\n"
        },
        {
          "code": "How old are you?\n19\nAllowed to drive a car in Texas: Yes\n"
        },
        {
          "text": "Show SolutionOn line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "list": [
            "On line 8,ageis defined as astd::uint8_t. Becausestd::uint8_tis typically defined as a char type, using it here will cause the program to behave as if we\u2019re inputting and outputting a char value rather than a numeric value. For example, if the user entered their age as \u201c18\u201d, only the character'1'would be extracted. Because1has ASCII value49, the user would be treated as if they were 49 years old. A regularintshould be used to store the age, as the age doesn\u2019t require a specific minimum integer width. We can also remove#include <cstdint>.On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").",
            "On line 13, we use the magic number16. Although the meaning of16is clear from the context it is used in, using aconstexprvariable with the value16should be preferred instead.On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\").",
            "On line 18,'.\\n'is a multicharacter literal that will print the wrong value. It should be double-quoted (\".\\n\")."
          ]
        },
        {
          "text": "Question #3What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "What are the primary differences betweenstd::stringandstd::string_view?What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "What can go wrong when using astd::string_view?Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Show Solutionstd::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it.Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "std::stringprovides a modifiable string. It is expensive to initialize and copy.std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it."
        },
        {
          "text": "std::string_viewprovide a read-only view of a string that exists elsewhere. It is inexpensive to initialize and copy.std::string_viewcan be dangerous when the string being viewed is destroyed before thestd::string_viewthat is viewing it."
        },
        {
          "text": "Question #4Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Write a program that asks for the name and age of two people, then prints which person is older.Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Here is the sample output from one run of the program:Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "code": "Enter the name of person #1: John Bacon\nEnter the age of John Bacon: 37\nEnter the name of person #2: David Jenkins\nEnter the age of David Jenkins: 44\nDavid Jenkins (age 44) is older than John Bacon (age 37).\n"
        },
        {
          "text": "Show HintHint: Input the person\u2019s name usingstd::getline()Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Show Solution#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "code": "#include <iostream>\n#include <string>\n#include <string_view>\n\nstd::string getName(int num)\n{\n    std::cout << \"Enter the name of person #\" << num << \": \";\n    std::string name{};\n    std::getline(std::cin >> std::ws, name); // read a full line of text into name\n\n    return name;\n}\n\nint getAge(std::string_view sv)\n{\n    std::cout << \"Enter the age of \" << sv << \": \";\n    int age{};\n    std::cin >> age;\n\n    return age;\n}\n\nvoid printOlder(std::string_view name1, int age1, std::string_view name2, int age2)\n{\n    if (age1 > age2)\n        std::cout << name1 << \" (age \" << age1 <<\") is older than \" << name2 << \" (age \" << age2 <<\").\\n\";\n    else\n        std::cout << name2 << \" (age \" << age2 <<\") is older than \" << name1 << \" (age \" << age1 <<\").\\n\";\n}\n\nint main()\n{\n    const std::string name1{ getName(1) };\n    const int age1 { getAge(name1) };\n    \n    const std::string name2{ getName(2) };\n    const int age2 { getAge(name2) };\n\n    printOlder(name1, age1, name2, age2);\n\n    return 0;\n}"
        },
        {
          "text": "Question #5In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "In the solution to the above quiz, why can\u2019t variableage1inmainbe constexpr?Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "Show SolutionA constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const.Next lesson6.1Operator precedence and associativityBack to table of contentsPrevious lesson5.9std::string_view (part 2)"
        },
        {
          "text": "A constexpr variable requires a constant expression initializer, and the call togetAge()isn\u2019t allowed in a constant expression. Therefore, we can only make the variable const."
        }
      ]
    }
  ],
  "quiz": []
}